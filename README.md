# 🤖 AI ChatBot with Context Memory

Telegram-бот на базе **aiogram 3.x** и **OpenAI API**, способный поддерживать контекст диалога, хранить историю сообщений в реляционной базе данных и корректно обрабатывать ошибки.

Проект разработан с соблюдением принципов **Clean Architecture**, типизации и покрытия ключевой логики тестами.

## ✨ Особенности

*   **Контекстная память**: Бот помнит историю переписки, используя её для формирования релевантных ответов.
*   **Надежное хранение**: История сообщений сохраняется в базе данных (**SQLite**) через **SQLAlchemy (Async)**. Используется нормализованная схема (таблицы `Dialog` и `Message`).
*   **Чистая архитектура**:
    *   Разделение ответственности: Transport Layer (`main.py`), Business Logic (`services/`), Data Access (`database/`).
    *   Внедрение зависимостей через сервисный слой.
*   **Управление состоянием**: Команда `/start` или кнопка в меню полностью очищают историю диалога.
*   **Обработка ошибок**: Обработка лимитов API, проблем с сетью и неверных конфигураций.
*   **Тестирование**: Покрытие unit-тестами (моки API) и интеграционными тестами (БД в памяти).

## 🛠 Стек технологий

*   **Язык**: Python 3.9+
*   **Фреймворк бота**: [aiogram 3.x](https://docs.aiogram.dev/) (асинхронный)
*   **База данных**: SQLAlchemy 2.0 (AsyncIO) + SQLite (для демонстрации) / PostgreSQL (для продакшена)
*   **AI Provider**: OpenAI API (совместим с OpenRouter, LocalLLM и др.)
*   **Конфигурация**: `python-dotenv`
*   **Тестирование**: `pytest`, `pytest-asyncio`

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
git clone https://github.com/MrTimofeev/tg_chatgpt_bot.git
cd tg_chatgpt_bot

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните его своими данными:

```ini
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_telegram_bot_token

# OpenAI Configuration
OPENAI_API_KEY=sk-your_openrouter_api_key
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free

# Database URL (SQLite по умолчанию)
DB_URL=sqlite+aiosqlite:///bot_database.db

# Настройки
MAX_CONTEXT_MESSAGES=10
```

### 3. Запуск бота

```bash
python -m src.bot.main
```
## 🧪 Запуск тестов

Проект покрыт тестами для проверки бизнес-логики и работы с БД.

```bash
# Запуск всех тестов с подробным выводом
pytest -v
```

## 🏗 Структура проекта

```text
src/
├── bot/
│   ├── config.py              # Управление конфигурацией и валидация .env
│   ├── prompts.py             # Системные промпты
│   ├── messages.py            # тексты сообщений
│   ├── keyboards.py           # Фабрика клавиатур
│   ├── main.py                # Точка входа и хендлеры (Transport Layer)
│   ├── database/
│   │   ├── session.py         # Настройка движка БД и контекстный менеджер
│   │   ├── models.py          # ORM модели (Dialog, Message)
│   │   └── crud.py            # Операции с данными (CRUD)
│   └── services/
│       ├── openai_service.py  # Низкоуровневый клиент для LLM API
│       └── dialog_service.py  # Бизнес-логика управления диалогами
tests/
├── conftest.py                # Фикстуры pytest
├── test_openai.py             # Unit-тесты сервиса OpenAI
└── test_database.py           # Интеграционные тесты БД
```

## 📝 Лицензия

MIT License.

---
*Разработано в качестве технического задания для демонстрации навыков Python Backend разработки.*
